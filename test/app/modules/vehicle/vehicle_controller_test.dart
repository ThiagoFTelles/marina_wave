import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:marinawave/app/modules/vehicle/vehicle_controller.dart';
import 'package:marinawave/app/modules/vehicle/vehicle_module.dart';

void main() {
  initModule(VehicleModule());
  // VehicleController vehicle;
  //
  setUp(() {
    //     vehicle = VehicleModule.to.get<VehicleController>();
  });

  group('VehicleController Test', () {
    //   test("First Test", () {
    //     expect(vehicle, isInstanceOf<VehicleController>());
    //   });

    //   test("Set Value", () {
    //     expect(vehicle.value, equals(0));
    //     vehicle.increment();
    //     expect(vehicle.value, equals(1));
    //   });
  });
}
