import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:marinawave/app/modules/vehicle/vehicle_page.dart';

void main() {
  testWidgets('VehiclePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(VehiclePage(title: 'Vehicle')));
    //  final titleFinder = find.text('Vehicle');
    //  expect(titleFinder, findsOneWidget);
  });
}
