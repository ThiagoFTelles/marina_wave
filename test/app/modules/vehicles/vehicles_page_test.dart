import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:marinawave/app/modules/vehicles/vehicles_page.dart';

void main() {
  testWidgets('VehiclesPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(VehiclesPage(title: 'Vehicles')));
    //  final titleFinder = find.text('Vehicles');
    //  expect(titleFinder, findsOneWidget);
  });
}
